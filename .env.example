# Entorno de la aplicación
NODE_ENV=development

# URL pública de la app (donde corre el frontend)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# URL de la API a la que se conecta el frontend
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# URL usada por NextAuth (debe coincidir con la del frontend en el entorno)
NEXTAUTH_URL=http://localhost:3000

# Secret usado por NextAuth (reemplazar con un string seguro en producción)
NEXTAUTH_SECRET=your_nextauth_secret_here

# Versión de la aplicación (toma la de package.json con npm_package_version)
NEXT_PUBLIC_APP_VERSION=$npm_package_version
